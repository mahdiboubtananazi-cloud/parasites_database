export interface Parasite {
  id: string;
  name: string;                // ????? ?????? (Scientific Name)
  type: string;                // ??????? (Nematode, etc.)
  stage: string;               // ??????? (Egg, Larva, etc.)
  description?: string;        // ?????
  image_url: string;           // ????? ??????? ?????? Supabase (snake_case)
  
  // ???? ?????? ????????
  common_name?: string;
  host?: string;
  location?: string;
  sample_type?: string;
  
  // ???? ??????
  created_at?: string;
  student_name?: string;       // ??? ?????? ???? ??? ??????
}

// ????? ?????? ???????? ?? ???? ???????
export interface ParasiteFilter {
  search: string;
  type: string | 'all';
  stage: string | 'all';
}
